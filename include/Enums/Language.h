#pragma once

// be aware that these language values directly
// reference their columns in the database.
// reordering this enum will cause confusion.
enum class Language {
	VOID,
	Lang_EN,
	Lang_DE,
	Lang_CH,
	LANG_ES,
	MAX
};

