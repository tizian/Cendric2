#pragma once

#include "global.h"

// some constants used by the GUI
class GUIConstants {
public:
	static float LEFT;
	static float TOP;

	static float TEXT_OFFSET;
	static float LABEL_OFFSET;
	static float BUTTON_MARGIN;

	static int CHARACTER_SIZE_S;
	static int CHARACTER_SIZE_M;
	static int CHARACTER_SIZE_L;
	static int CHARACTER_SIZE_XL;

	// colors for windows
	static sf::Color MAIN_COLOR;
	static sf::Color BACK_COLOR;
	static sf::Color ORNAMENT_COLOR;
};
